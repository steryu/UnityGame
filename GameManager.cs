/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    PlayerStats playerStats;
    void Start()
    {
        InitializePlayerStats();
*//*        LoadPlayerStats();*//*
    }

    void InitializePlayerStats()
    {
        playerStats = new PlayerStats();

        *//*playerStats.BaseHealth = 100f;
        playerStats.HealthRegeneration = 1f;
        playerStats.BaseDefense = 10f;
        playerStats.DodgeChance = 1f;

        playerStats.BaseDamage = 10f;
        playerStats.AttackSpeed = 1f;
        playerStats.AttackRange = 5f;
     
        playerStats.CriticalHit = (playerStats.BaseDamage * playerStats.CriticalDamage);
        playerStats.CriticalDamage = 2f;
        playerStats.CriticalHitChance = 5f;

        playerStats.MovementSpeed = 10f;
        playerStats.GoldGain = 0;
        playerStats.ExperienceGain = 0f;
        playerStats.PickupRadius = 0f;*//*
    }
}
*/